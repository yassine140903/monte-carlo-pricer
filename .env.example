# --- Database ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=monte_carlo_pricer
DB_USER=postgres
DB_PASSWORD=postgres

# --- Data ---
# Number of years of history to backfill on first fetch for a ticker
DEFAULT_LOOKBACK_YEARS=3

# --- MLflow ---
MLFLOW_TRACKING_URI=http://localhost:5000
