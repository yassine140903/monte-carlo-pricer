# Base URL the dashboard calls. Only needed for local development outside
# Docker, where nothing proxies /api; copy this file to .env to use it.
# Leave unset in the Docker image so the app falls back to the relative
# "/api" that Nginx forwards to the backend service.
VITE_API_BASE_URL=http://localhost:8000
